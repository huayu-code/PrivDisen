# ==============================================================
# PrivDisen Dependencies
# ==============================================================
#
# IMPORTANT: Install PyTorch FIRST (with CUDA support if you have GPU).
# The lines below only specify version ranges; they do NOT select the
# correct CUDA variant. Run ONE of the following BEFORE pip install -r:
#
#   # GPU - CUDA 11.8
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
#
#   # GPU - CUDA 12.1
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
#
#   # CPU only
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cpu
#
# Then run:
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ==============================================================

# Core (skip if already installed above)
torch>=1.13.0,<2.5.0
torchvision>=0.14.0,<0.20.0
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Configuration
pyyaml>=6.0
omegaconf>=2.3.0

# Data & ML
scikit-learn>=1.0.0
pandas>=1.3.0

# Dataset Download (China mirror acceleration)
modelscope>=1.10.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# Logging & Experiment Tracking
tensorboard>=2.11.0
tqdm>=4.64.0

# Reproducibility
jsonargparse>=4.20.0
